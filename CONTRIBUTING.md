# 贡献指南

感谢参与 ContentX。提交变更前，请先阅读[产品需求](./docs/PRD.md)、[项目状态](./docs/STATUS.md)和[路线图](./docs/ROADMAP.md)，确认变更属于当前范围。

## 开发环境

- Go 1.25+
- Node.js 20
- npm
- Docker（数据库、Redis 或外部服务集成测试）

安装前端依赖：

```bash
cd web
npm ci
```

## 工作流

1. 从最新目标分支创建主题分支。
2. 一个分支只处理一个清晰主题。
3. 先添加失败测试或可复现步骤，再实现修复。
4. 不覆盖或回滚与当前任务无关的工作区改动。
5. REST、配置、迁移和用户行为变化必须同步文档。
6. 提交前完成本地验证。

推荐分支名：

- `feat/<topic>`
- `fix/<topic>`
- `docs/<topic>`
- `refactor/<topic>`

## 代码要求

### Go

- 使用 `gofmt`
- 错误必须带上下文，不记录密钥或 Token
- 权限在业务层复核，不能只依赖菜单或路由中间件
- 数据库变更使用正式迁移，并覆盖空库与升级库
- 外部请求必须有超时、错误处理和可测试的降级路径

### 前端

- TypeScript 不使用无依据的 `any`
- 服务器状态优先使用 Vue Query
- 权限 slug 使用统一类型和注册表
- 页面必须处理加载、空数据、失败、拒绝和保存状态
- 不加入硬编码统计结果或不可用假入口

## 测试

完整检查：

```bash
make check
```

浏览器 E2E：

```bash
cd web
npm run test:e2e
```

涉及 Redis、S3、数据库恢复或 OpenTelemetry 的变更必须运行真实服务测试。具体方法见[标准操作流程](./docs/SOP.md)。

最低测试要求：

- 新功能：成功、失败和权限拒绝路径
- Bug 修复：能够在修复前失败的回归测试
- 数据迁移：新库、升级库及必要的回滚说明
- REST：OpenAPI 重新生成且无漂移
- 前端交互：单测；关键用户流程补充 E2E

## 文档

文档职责见[文档中心](./docs/README.md)。不要在多个文件重复维护同一状态：

- 当前事实写入 `STATUS.md`
- 产品承诺写入 `PRD.md`
- 未完成计划写入 `ROADMAP.md`
- 操作命令写入 `SOP.md`
- 用户可见变化写入 `CHANGELOG.md`

已完成或被替代的方案移入 `docs/archive/`，并在归档索引说明原因和日期。

## Pull Request

PR 描述应包含：

- 问题与目标
- 主要实现
- 风险与兼容性
- 测试证据
- 迁移、配置和文档变化

不要在同一 PR 混入无关格式化、生成物或重构。涉及安全漏洞时不要创建公开 PR，按[安全策略](./SECURITY.md)报告。
