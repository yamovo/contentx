# 安全策略

## 支持范围

安全修复优先提供给最新正式版本和 `main` 分支。更早版本可能需要先升级才能获得修复。

| 版本 | 支持状态 |
|---|---|
| 最新正式版本 | 支持 |
| `main` | 开发中，接受报告 |
| 更早版本 | 尽力支持，不保证单独回补 |

## 报告漏洞

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私下提交 GitHub Security Advisory。

不要通过公开 Issue、Discussion、PR、日志粘贴站或社交媒体披露尚未修复的漏洞。

报告应尽量包含：

- 受影响版本或 Git SHA
- 部署方式和相关配置（移除真实密钥）
- 漏洞类型与安全影响
- 最小复现步骤或测试
- 预期行为与实际行为
- 建议修复方向（可选）

严禁提交：

- 真实密码、JWT、API Token、TOTP secret 或云凭据
- 未经授权获取的个人数据或客户内容
- 会对第三方系统造成持续影响的利用脚本

## 响应流程

维护者将：

1. 确认收到报告并进行初步复现。
2. 评估严重程度、影响版本和缓解措施。
3. 在私有分支准备修复与回归测试。
4. 协调披露日期和版本发布。
5. 发布后在变更日志中说明影响和升级建议，但不公开可直接滥用的细节。

## 安全边界

以下行为不应被视为漏洞：

- 管理员在拥有明确权限时修改内容或配置
- debug 模式使用自动生成的临时密钥
- 已在[项目状态](./docs/STATUS.md)明确记录且不造成越权的数据或功能限制
- 仅影响已停止支持版本的问题

如果限制可以导致越权、数据泄露、代码执行、认证绕过或数据完整性破坏，仍应按漏洞私下报告。

## 部署责任

生产部署方必须：

- 使用强随机密钥并安全轮换
- 启用 TLS 和可信反向代理配置
- 限制数据库、Redis、对象存储和监控端口
- 定期执行备份恢复演练
- 对 R2、AWS 等目标 S3 供应商运行上线前兼容测试
- 及时升级依赖和受支持版本

详细操作见[标准操作流程](./docs/SOP.md)。
